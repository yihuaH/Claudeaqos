# 2026-09-18 (周五) 收盘后收尾 wrapup 17:45 ET

status: completed
phase: wrapup (当日 preclose 已 completed, 未触发 fail-safe); `timing.total_seconds = 10.3s`

## 1. 当日实盘动作: 零

0 出场 · 0 买入 · 0 期权单。原因见 `journal/2026-09-18-preclose.md` §4
(可用资金 −$1.59, 13 个信号全跳过)。

## 2. ⚠️ 用户指令的提前平仓 **未执行**

15:36 ET 用户说「现在就平掉, 把参数给我」, 会话按 playbook 4D 手动通道给出完整参数
(买回 IWM 09/23 $276P + 卖出 $250P, 净借记限价 $0.19→$0.20, 预估 +$75~76)。

**17:45 核查: `get_option_orders(created_at_gte=2026-09-18)` 返回空数组 —— 全天零期权订单,
用户未下单。** 价差仍持有 (券商 `options_value = −$17`)。

处置: 不追问、不代下 (多腿 agentic 通道不支持, 且开/平仓均须用户 App 手动)。
按 playbook §4D「平仓两步通知规范」, **本战报即为第①步预告**, 第②步执行指令将在
**下周一 (09-21) 盘中主动发出** —— 届时正好也是引擎排定的 `DTE≤2` 强平日, 两者重合。

## 3. 前置闸

- `position_check`: **ok, 8/8 一致**
- `price_check`: **ok — 10/10 完全一致, 0.0 bp**
  ⚠️ **连续第 2 日**: 17:45 ET 取到的券商官方 `close` 仍是**上一交易日** (今日取到 09-17)。
  已可确认为常态而非偶发 —— Robinhood 官方收盘在 17:45 ET 尚未落库。
  影响: price_check 恒滞后一天, 校验仍有效 (按 broker 自带 date 对齐引擎同日日线), 但
  **当日行情管道的同源性要到次日才验得到**。建议: 文档化这一滞后即可, 不值得为它改时刻表。

## 4. ⚠️ anomalies = 1 (第 4 日) —— 今天它**真的咬了**

```
ch.config 失败 (rc=1): 无活跃挑战者
```

机制同前 (未写新 `ch_config.json` → `ch.signal` 读 9-15 那份被否决参数, `stop_loss_pct = 3.0`)。
**前三日该参数未实际触发, 今天触发了**:

| | 挑战者 (paper, 被否决参数) | 实盘/冠军 |
|---|---|---|
| RVMD 入场 | $200.5144 (09-17, Alpaca 次开成交) | $195.12 (09-17 15:30) |
| 09-18 收盘 | $190.96 | $190.96 |
| 相对入场 | **−4.76%** | −2.13% |
| 止损线 | 3% → **$194.50 已击穿** | 7% → $186.48 未触及 |
| 今日动作 | **卖出 `stop_loss`** | 继续持有 |

即: 被否决的 3% 止损今天砍掉了一只冠军参数会继续持有的票。**这是该缺陷的第一个实际分歧样本。**

**危害边界 (查证后修正我此前的担心)**: `learn.py record` 每日把 paper 净值写进
`state/learning.json` 的 `equity_history`, 但 `_evaluate` 只取 `date >= challenger.started`
的片段 —— **今天这些行不会污染未来挑战者的 TWR 评估**。真实代价只有一条:
paper 账本的**持仓与成交历史**正按错误规则漂移, 新挑战者启动时会继承一个用错参数搭出来的组合。

三个修法仍等用户定 (① `phase_paper` 无挑战者时整段跳过 [我倾向] ② `ch.config` 失败即跳过后续
`ch.*` ③ 改 rc 为 0 [不推荐])。**本会话不自行改动** —— 这是策略行为变更, 不是我该单方面做的决定。

## 5. 组合 (收盘)

净值 **$10,059.29** · 持仓市值 $7,464.24 · 现金 $2,603.41 · 期权 −$17
持仓浮盈 **−$120.26 (−1.59%)** · 回撤 **3.63%** (HWM $10,438.52)

| 标的 | 均价 | 收盘 | 浮盈% | 今日% |
|---|---|---|---|---|
| BNY | 157.21 | 153.72 | −2.22% | +0.46% |
| BAC | 59.08 | 57.70 | −2.34% | −0.83% |
| WELL | 233.23 | 228.72 | −1.93% | −1.52% |
| LNG | 271.00 | 268.24 | −1.02% | −0.60% |
| IWM | 284.87 | 284.09 | −0.27% | −0.47% |
| RVMD | 195.12 | 190.96 | −2.13% | **−3.04%** |
| CVS | 90.75 | 88.82 | −2.12% | −1.61% |
| BMY | 62.89 | 63.03 | +0.23% | +0.31% |

8 只里 6 只浮亏, 最深 BAC −2.34%。无一接近 7% 止损线。

## 6. 实盘周期权

**IWM 276/250** 仍持有 (09-15 开, 到期 09-23, **DTE 5 → 周一 2**)。
收盘净 mark $0.17 → 浮盈 **+$78.00** (占抵押 $2,509 的 +3.11%)。
已实现 5 回合 / 5 胜 / +$385.00 不变。

## 7. paper 轨道

挑战者: 卖 DIA(time_stop) · **RVMD(stop_loss, 见 §4)**; 买 MDLZ $9,669 · VZ $452。
动量轮动: `rebalance_due=false` (上次 09-14, 4 日)。

## 8. 下周一 (2026-09-21) 三件事

1. **13:00 股票池刷新** (100 只), 须在 15:20 主跑前 finalize (仅 2h20m 余量)
2. **IWM 期权 DTE≤2 强平日** —— 会话将在盘中主动发第②步执行指令; 平掉才释放 $2,509 抵押,
   `buying_power` 才从 $3.41 恢复, 股票轨道才重新买得起东西
3. 加仓线最近: RVMD $190.96 vs ≤$189.27 (差 **0.89%**) · WELL $228.72 vs ≤$226.23 (差 1.10%)
