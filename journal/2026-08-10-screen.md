# 2026-08-10 (周一) 周度股票池刷新

status: completed
运行时间: 15:08–15:2x ET（主跑 17:45 之前完成，主跑将用上新池）
上次刷新: 2026-08-03（间隔 7 天，符合 `screen.json refresh_days`）

## 管线执行

| 步骤 | 结果 |
|---|---|
| 数据源自诊断 | `all_ok: true`（FRED VIX 14.9 / Alpaca 开市中） |
| a. 全市场正股 | 14,207 活跃 → **5,320** 只（名称启发式剔基金） |
| b. 40 日线 + 流动性筛 | 5,317 只有数据 → **pool 1,000**（池内最低日均成交额 $79.5M） |
| c. 470 日线 | 1,000/1,000 全部有数据 |
| d. Robinhood「100 most popular」 | 100 只，用于 `popular_bonus` 加分 |
| e. rank | 存活 **782**（剔除: 异动 169 / 波动 27 / 历史不足 17 / 价格 5）→ 候选 **150** |
| f. 行业归类 | 150/150 全部有行业（见下方口径说明） |
| g. finalize | **100 只**，跳过 5（行业超限） |
| h. 校验 | 数量 ✅ 无重复 ✅ 行业 ≤20 ✅ generated ✅ 每只有行业 ✅ |

### 行业数据口径（本次优化，无参数变更）

`get_equity_fundamentals` 每次返回含全文公司简介，150 只需 15 次调用会占用大量上下文。
本次改为**分层复用**（行业分类本身稳定，不随周变化）：

- 91 只复用 `universe.json` 既有 `sectors` 字段（仓库内权威记录）
- 26 只复用上轮 MCP 取回后固化的映射表
- **33 只本轮实际调用 MCP 取回**（4 批）

结果 150/150 全覆盖，MCP 调用从 15 次降到 4 次。**筛选参数与打分逻辑一字未改**（红线2）。

## 池变动: 新增 23 / 剔除 23

**新增**: ADP · ALL · AME · AMP · CNC · DINO · EMR · EXPE · FAST · FCX · FTNT · GD · KLAC ·
NET · OXY · PATH · PAYX · REGN · RY · TD · TPR · VRTX · WBS

**剔除**: AAL · BP · CFG · CMI · CNQ · **D** · EQIX · FIX · GOOG · GOOGL · HST · MPWR · NTRA ·
PBR · RCL · ROK · **SU** · TRGP · URI · USFD · WFC · WMT · XYZ

### ⚠️ 持仓中有 2 只被剔除出池

当前持仓 5 只（CVS / **D** / F / MNST / **SU**），其中 **D 和 SU 本次被剔除出候选池**。

**按规则不强制卖出** —— 池只决定"能买什么"，不决定"何时卖"。D 和 SU 继续持有，
由 `signals.py` 的正常出场逻辑（`close>SMA5` / `RSI2≥65` / 7% 止损 / 10 日时间止损）走完。
影响仅在于：出场后不会再被重新买入，且**加仓机制对它们仍然有效**（加仓针对已持仓位，
不经过池筛选）。

D 当前加仓线 ≤67.33，SU ≤59.04 —— 这两条监控继续有效。

## 行业结构变动

| 行业 | 旧 | 新 | 变动 |
|---|---|---|---|
| Technology Services | 4 | 7 | **+3** |
| Electronic Technology | 14 | 16 | +2 |
| Health Services | 2 | 3 | +1 |
| Health Technology | 12 | 13 | +1 |
| Non-Energy Minerals | 3 | 4 | +1 |
| **Energy Minerals** | 10 | 8 | **−2** |
| **Utilities** | 3 | 1 | **−2** |
| Producer Manufacturing | 8 | 7 | −1 |
| Transportation | 5 | 4 | −1 |
| Commercial Services | 1 | 0 | −1 |
| Industrial Services | 1 | 0 | −1 |
| Finance | 20 | 20 | 持平（触及上限 20） |

科技类整体 +5、能源与公用事业 −4，反映近 1 年趋势打分的轮动。Finance 仍顶在
`max_per_sector_in_universe = 20` 的上限。

## 合规确认

- 本任务**未下任何单**，**未改** `state/positions.json` 等任何交易账本
- 仅写 `strategy/universe.json` + `strategy/stocks.json`（后者由 finalize `--apply-stocks` 同步）
- 筛选只决定"能买什么"，何时买/买多少仍由 `signals.py` 决定（红线2）
