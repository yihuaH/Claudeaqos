# 2026-09-16 (周三) 收盘后 wrapup 17:45 ET

status: completed

## 1. 驱动器

`--phase wrapup`, `timing.total_seconds = 11.2s`, `fatal=null`, **anomalies=1** (见 §3)。

| 闸 | 结果 |
|---|---|
| `price_check` | **ok — 20/20 完全一致, mean/max 偏差 0.0 bp** |
| `position_check` | **ok — 7/7 一致** (NSC 4.178761 已含今日加仓) |

## 2. 账户

净值 **$10,032.70** · 现金 $2,799.89 · `buying_power` **$199.89** ·
`unleveraged_BP` 等于 BP → 无借贷。期权市值 −$87 (IWM 价差负债端)。

策略仓 7 只: NSC(2档) · BNY · BAC · WELL · LNG · PFE · IWM
实盘期权 1: IWM260923P00276000 credit_put_spread

## 3. ⚠️ 异常: `ch.config 失败 (rc=1): 无活跃挑战者` — 且后果比表面严重

### 3.1 先更正我昨天的一句话

昨天否决挑战者前我在副本上做了验证, 并报告「无挑战者时 `challenger-config` 打印
『无活跃挑战者』**rc=0**, 主跑不会崩」。**rc=0 这个结论是错的** —— 当时的测试写成
`... 2>&1 | tail -3 && echo "rc=$?"`, 取到的是 `tail` 的退出码而非 python 的。
实测 `challenger-config` 在无挑战者时**退出码为 1**。

「主跑不会崩」这半句仍成立 (daily.py 对该步用 `critical=False`, 今日 price_check /
position_check / 动量全部正常), 但**每日会多出一条固定的 anomaly**, 长期会钝化红线6 的告警通道。

### 3.2 真正的问题: 挑战者今天跑在**被否决的参数**上

`ch.config` 失败 → 没写出新的 `ch_config.json` → 后续 `ch.signal --config <workdir>/ch_config.json`
**读到的是昨天残留的那份**:

```
文件时间戳: Sep 15 21:46  (昨日 wrapup 产出)
exit.stop_loss_pct: 3.0   ← 正是 2026-09-15 被正式否决的挑战者参数 (冠军为 5.0)
```

今日 `ch.signal` rc=0、`ch.run` rc=0 —— 也就是说**挑战者纸面轨道照常出单并成交了, 用的是
已被否决的 3% 止损**。今日出 2 卖 (DIA time_stop · UNP stop_loss) + 4 买
(RVMD/XLF/CSX/UNP; 其中 UNP 同日买卖冲突已被 `_wash_conflicts` 正确拦下)。

**影响面**: **仅 paper, 不涉真金**。但会持续污染挑战者账本, 且轨道在"无挑战者"状态下
仍以僵尸参数运行。

### 3.3 还有一层隐性非确定性

`ch_config.json` 在 **scratchpad** 里。若容器重建 (本会话近两周已发生数次), 该文件不存在,
`ch.signal` 会直接失败 → 轨道静默停跑。也就是说**同一状态下的行为取决于临时目录是否幸存**,
这本身就不该出现在确定性管线里。

### 3.4 未自行修复 (红线6)

正确行为应是: 无活跃挑战者时**整段跳过**挑战者轨道, 而不是跑在陈旧文件上。
`daily.py phase_paper` 目前只按 `learning.json enabled` 开关 (=true), 不看是否真有挑战者。

候选修法 (待用户批准):
1. `phase_paper` 的挑战者段增加「无活跃挑战者则整段跳过」的前置判断 (最小改动)
2. `ch.config` 失败即跳过后续 ch.* 步骤 (等价, 但把判断留在失败处)
3. `challenger-config` 无挑战者时改为 rc=0 + 显式标记 —— **不推荐**, 会把"没有挑战者"
   伪装成正常, 反而更难发现

## 4. 纸面轨道

- **挑战者**: 净值 $97,685.93。轨道状态见 §3 —— 本日数据**不应计入任何评估**。
- **动量轮动**: 非调仓日 (距上次 2 天)。
- **周call paper**: 持仓 4 张, 无出场。

## 5. 三处修补运行状况

- **修法1** (跨日 context): 连续第 4 日无相关异常 ✅
- **修法3** (成交日): NSC 加仓为当日成交, `fill_date == today`, `entry_date` 正确保留
  首次入场日 2026-09-04 ✅
- **修法2** (排队卖单升市价): 仍未触发
