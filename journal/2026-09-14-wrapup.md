# 2026-09-14 (周一) 收盘后 wrapup 17:45 ET

status: completed

## 1. 驱动器结果

`--phase wrapup`, `timing.total_seconds = 13.1s`, **fatal=null, anomalies=0**。
正股/期权信号未重算 (盘前已完成), 本阶段只跑纸面轨道 + 行情核对。

| 闸 | 结果 |
|---|---|
| `price_check` | **verdict=ok — 20/20 完全一致, mean_abs_dev 0.0 bp, max 0.0 bp** ("行情管道同源") |
| `position_check` | verdict=ok — 5/5 券商持仓与账本完全一致 |
| 数据源 | alpaca ok · fred ok |

行情管道交叉核对**首次在密钥恢复后跑通**, 20 只全中零偏差, 证明 SIP 口径未退回 iex。
(比的是 09-11 官方收盘 —— 今日收盘 17:47 尚未结算, 与既往 wrapup 同口径。)

## 2. 账户

净值 **$10,081.66** · 现金/BP **$4,601.70** · `unleveraged_buying_power` 等于 BP (无借贷)。
策略仓 5 只: MMM · UNP · NSC · BNY · BAC。

## 3. 纸面轨道

- **挑战者**: 净值 $98,742.14。今日出 8 卖 (DIA time_stop · RTX/NSC stop_loss · DXCM/LLY/MSI/DASH/ADP
  exit_strength) + 3 买 (BNY/BAC/WELL)。`evaluate` **verdict=extend**:
  39 天 · 163 笔 · live +83.255% vs paper −2.117% · **edge −85.372%** · paper 最大回撤 8.546%。
  挑战者持续大幅落后冠军, 继续验证期, 不晋级。
- **动量轮动**: 调仓日 (距上次 6 天), 出 QQQ 卖单 1 笔; 排队限价下买不起 1 整股被跳过。
- **周call**: wrapup 阶段**不跑**该轨道 (命令清单无 wc.*), 故盘前那笔异常未在本阶段处理 —— 见 §4。

## 4. ⚠️ 确认: paper 周call 出现孤儿仓 (XLI 同款路径, 仅纸面)

### 事实链 (全部来自一手数据)

| 时间 | 事件 |
|---|---|
| 09-10 收盘后 | 引擎出 2 笔 paper 周call 买单, 收盘后以 limit/day 排队至次开: `AMZN260918C00220000` ($32.48) 与 `NVDA260925C00195000` ($24.39, spot 218.36, rsi2 9.23) |
| 09-11 13:30 UTC (09:30 ET) | 两单提交。**AMZN 全天未成交 → expired** |
| 09-11 **15:22 ET** | 当日盘前主跑执行。此时 NVDA 尚未成交 |
| 09-11 **15:28 ET** | **NVDA260925C00195000 成交 @ $25.10** (券商 `filled_at=2026-09-11T19:28:04Z`) —— **比当日盘前主跑晚 6 分钟** |
| 09-11 盘前主跑同时 | 把 `state/weekly_call_last_orders.json` **覆写为 buys=[] sells=[]** (当日无周call 出单) |
| 09-11 17:45 | wrapup 因 Alpaca 401 停跑 —— **唯一能当日回收该成交的窗口消失** |
| 09-14 15:22 | 盘前 `wc.paper.apply_sync` 捞到该成交, 但 context 已是 09-11 的空版本 → 报错 `开仓成交在 --context 里找不到对应买单`, 成交**回不到账本** |

### 现状 (Alpaca paper 账户逐仓核对)

| OCC | 券商 | 账本 | 判定 |
|---|---|---|---|
| `BAC260925C00051000` | qty 1 @ $8.35 | ✅ 有 | 一致 |
| `NVDA260923C00185000` | qty 1 @ $27.85 | ✅ 有 | 一致 |
| **`NVDA260925C00195000`** | **qty 1 @ $25.10, 市值 $1,260, 未实现 −$1,250** | ❌ **无** | **孤儿** |

幽灵仓 (账本有券商无): 无。

### 为什么危险

孤儿仓对引擎**不可见** → 永远不会产生出场信号 → 一路持到 **2026-09-25 到期**。
NVDA 现价约 $211 vs 行权价 $195, **当前 ITM 约 $16**。若到期仍 ITM 会自动行权成 **100 股 NVDA
(约 $21,000 名义)**, 而该 paper 账户现金仅 $313.73 —— 这正是 XLI 那笔 (到期 ITM→自动行权→孤儿正股,
最终 −$1,076) 的同一条路径, 只是规模更大。DTE 11, 不是今晚的急事, 但有明确期限。

### 根因 (与 09-09 那次不同, 是新的一条)

`weekly_call_last_orders.json` 是**每日整体覆写**的单文件 context。收盘后排队的订单若在
**次日或更晚**成交, 其开仓 context 早已被当日的新版本冲掉 —— 09-09 修的是"会话漏传 `--context`",
**修不到这条**。本次还叠加了 09-11 wrapup 因密钥故障停跑, 失去当日回收窗口。

### 处置 (按红线6: 只报告, 未改账本、未交易)

**可以干净复原** —— 所有字段都有一手来源, 无需杜撰:
- 开仓 context: git `570e608` 的 09-10 版本 (spot 218.36 · rsi2 9.23 · entry_quote
  {bid 24.31, ask 24.48, mid 24.395, spread 0.697%} · model_price 25.3333 · dte 15)
- 实际成交价: 券商 **$25.10** (非引擎 est 24.39)

但**写进账本等于把它交给全自动出场逻辑**, 所以不自行决定, 待用户示下。三个选项:

1. **补录账本** (按上述一手字段 + 实际成交价 $25.10), 之后引擎照常管它、到点自动平仓 —— 恢复正轨
2. **直接平掉** (paper, 不涉真金), 认 −$1,250, 账本记一笔孤儿冲销
3. 暂不动, 但**必须在 09-25 到期前处理**, 否则重演 XLI

同时建议修根因 (与既有待办 修法2/修法3 同一族, 需用户批准):
**排队未成交的订单, 其 context 不得被次日覆写** —— 例如 context 按 OCC 分键累积、或排队清单
自带 entry context 随成交一起回收。

---

## 5. 孤儿仓已补录 (用户回「1」批准)

按选项 1 执行: 把 `NVDA260925C00195000` 补进 `state/weekly_call_positions.json`,
交回引擎管理。**未交易, 未改动任何其它字段。**

### 补录字段与一手来源 (逐条可追溯, 无杜撰)

| 字段 | 值 | 来源 |
|---|---|---|
| `entry_premium` | **25.10** | 券商 Alpaca paper `/v2/orders` 实际成交价 (非引擎 est 24.39) |
| `entry_date` | **2026-09-11** | 券商 `filled_at = 2026-09-11T19:28:04Z` = 15:28 ET |
| `entry_underlying` | 218.36 | git `570e608` 的 09-10 引擎 context |
| `entry_rsi2` | 9.23 | 同上 |
| `entry_quote` | bid 24.31 / ask 24.48 / mid 24.395 / spread 0.697% | 同上 |
| `model_price` | 25.3333 | 同上 |
| 其余 (underlying/strike/expiry/contracts) | NVDA / 195.0 / 2026-09-25 / 1 | OCC 代码 + 券商持仓 |

est 24.39 → 实际 25.10 的 **+2.9%** 差价 = 收盘后排队限价挂到次开的滑点, 如实记在 `_recovered.note`。
`ledger_corrections` 追加一条, 记事实链、根因、批准来源。

### 补录后核对 (与券商逐仓比对)

```
✅ BAC260925C00051000    qty 1/1   avg $8.35/$8.35
✅ NVDA260923C00185000   qty 1/1   avg $27.85/$27.85
✅ NVDA260925C00195000   qty 1/1   avg $25.10/$25.10
孤儿: 无    幽灵: 无
```

`weekly_calls.py report` 确认引擎已看见该仓 (dte 11, entry_date 2026-09-11)。

### 引擎接管后的出场路径 (XLI 路径已切断)

| 闸 | 参数 | 对本仓的推演 |
|---|---|---|
| `force_exit_dte_lte` | 2 | 到期 09-25 → **约 09-23/24 强制平仓, 早于到期** ← 关键: 切断"到期 ITM→自动行权→孤儿正股" |
| `underlying_stop_loss_pct` | 5.0 | 止损线 = 218.36×0.95 = **$207.44**; NVDA 今收 $210.89, **距触发仅 +1.66%** ⚠️ |
| `max_holding_days` | 10 | 自 2026-09-11 起算 |
| `rsi2_min` | 65.0 | 止盈 |

⚠️ **近期极可能先被止损带走**: NVDA 只要再跌 1.66% 就触发 5% 止损线。这是引擎的正常行为
(红线2 不得干预), 只是提前说明 —— 该仓当前浮亏约 −$1,250 (−49.8%), 止损成交即坐实。

### 残余风险 (未消除, 与既有待办同族)

出场单若在**收盘后**产出, 走的是 limit/day 排队至次开 —— 正是 XLI 那笔没卖掉的机制 (待办「修法2」
未实施)。缓解在于: **paper 周call 轨道现在跑在 15:20 ET 盘前段** (本日 `wc.paper.run` 耗时 11.1s,
市场开着), 出场单是盘中真实单而非排队限价; 只有盘前段挂掉退化到 wrapup 时才会退回排队路径。

### 根因仍未修 (待用户批准)

`weekly_call_last_orders.json` 每日整体覆写 → 跨日成交的排队单必丢 context。
建议: context 按 OCC 分键累积 (只增不覆写, 成交后清), 或让排队清单自带 entry context 随成交回收。
与既有待办「修法2」(排队限价出场未成交→升市价) 是**两条不同的洞**, 都还开着。
