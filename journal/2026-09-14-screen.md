# 2026-09-14 (周一) 周度股票池刷新 — 数据源不可用, 未刷新

screen: halted_data_source
(未产出新池; `strategy/universe.json` **未改动**, 仍是 2026-09-07 生成的那 100 只, 继续有效)

## 1. 停在哪一步

前置检查第 2 项 (数据源自诊断) 未过, 管线 a 步之前就停:

```
alpaca: {"ok": false, "reason": "HTTP Error 401: Unauthorized"}
fred:   {"ok": true, "vix": "15.84", "date": "2026-09-11"}
```

任务指令原文: 「数据源自诊断 `python3 scripts/integrations.py status` (Alpaca 须 ok;
不可用则不刷新、写日志、通知用户结束)」—— 照此结束。

即便跳过这道闸也跑不动: 管线 a (`integrations.py assets`)、b/c (`bars` 两轮)、
e (rank 依赖 b/c 的产出) **全部**走 Alpaca, 四步连着断。

## 2. 幂等状态

`universe.json generated = 2026-09-07` ≠ 今天 → **不是**幂等跳过, 是真的没刷成。
下周一 (09-21) 的例行刷新会照常尝试; 若本周内密钥恢复, 也可手工补跑。

## 3. 故障延续情况 (本会话第 3 个检查点)

| 时间 (ET) | 场合 | Alpaca |
|---|---|---|
| 09-11 15:22 | 收盘前主跑 | ✅ 正常 (bars 取全) |
| 09-11 17:48 | 收盘后 wrapup | ❌ 401 → 停跑 |
| 09-14 10:45 | 晨间核查 | ❌ 401 |
| 09-14 13:08 | 周度股票池刷新 | ❌ 401 |

环境变量三次探测**逐字节相同** (key len=26 `PK…` / secret len=44 `5j…`) → 密钥未更换。

## 4. 影响

- **本任务**: 股票池停在 09-07 版。池子只决定"能买什么", 买卖时机仍由引擎定 (红线2);
  旧池 100 只依然可用, **迟一周刷新本身不造成交易风险**。
- **今天 15:20 ET 收盘前主跑**: 同样会在 preflight 401 停跑 —— 这才是今天的真问题
  (当日无信号、**无出场评估**)。详见 `journal/2026-09-14-morning.md` §2。
- 已持仓被剔除出池的情形本任务不处理 (本次也没产生新池, 无从谈起)。

## 5. 未通知第二次的理由

今日 10:47 ET 已就同一根因推送过手机通知, 且需要用户做的动作完全相同 (重新生成 Alpaca
纸面密钥)。同一根因 2 小时内重复推送只增噪音, 故本次只写日志 + 对话内报告。

---

## 6. 后续: 密钥已恢复 (2026-09-14 13:56 ET)

用户在 claude.ai/code 的环境变量里换上了新的 Alpaca 纸面密钥。

**本会话仍 401 属预期**, 非用户操作有误 —— 官方文档 (`docs/en/cloud-environments#set-environment-variables`)
写明「Each session copies the environment's values once, at startup… sessions already running keep the
values they started with」。本会话 09-11 启动, 持有的是旧快照, 且无法重读配置。

注意: 新生成的 Alpaca 纸面密钥同样是 `PK` 开头、同样 26/44 字符, **长度与前缀分辨不出新旧**,
因此不能靠本会话的 env 判断用户是否改成功。

验证办法: 另起一个云会话 (启动时会拷贝当前环境变量) 直接打两个端点:

| 端点 | 本会话 (旧快照) | 新会话 (新密钥) |
|---|---|---|
| `paper-api/v2/clock` | 401 | **200** |
| `data/v2/stocks/bars` | 401 | **200** |

→ **新密钥有效**。15:20 ET 的收盘前主跑是 Routine 唤醒的新会话, 会拿到可用密钥, 照常跑。
探测会话已归档。

⚠️ 但**本会话 (session_01NYPNGq5j25cGqpoLrWiA7t) 自此对 Alpaca 永久失能** —— 任何需要 Alpaca 的
补跑 (如 09-11 wrapup 补做、本周度股票池补刷) 都**不能由本会话执行**, 必须交给新会话。
