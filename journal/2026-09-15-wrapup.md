# 2026-09-15 (周二) 收盘后 wrapup 17:45 ET

status: completed

## 1. 驱动器

`--phase wrapup`, `timing.total_seconds = 9.9s`, **fatal=null, anomalies=0**。

| 闸 | 结果 |
|---|---|
| `price_check` | **ok — 20/20 完全一致, mean/max 偏差 0.0 bp** |
| `position_check` | **ok — 7/7 券商持仓与账本完全一致** |
| 数据源 | alpaca ok · fred ok |

## 2. 账户

净值 **$10,090.90** · 现金 $3,794.77 · `buying_power` **$1,194.77**
(= 现金 − 期权抵押 $2,600) · `unleveraged_BP` 等于 BP → **无借贷**。
期权市值 −$87 (卖出价差的负债端)。

策略仓 **7 只**: NSC · BNY · BAC · WELL · LNG · PFE · IWM
期权实盘仓 1: IWM260923P00276000 credit_put_spread (贷记 $0.95)

## 3. ⚠️ 挑战者验证期满 — **否决** (红线8)

`learn.py evaluate` 判 **verdict=fail**:

```
reason: 验证期满仍未同时满足 交易数/超额收益 标准
started 2026-07-16 · days 40 (max_paper_days 上限 40, 已满)
paper_trades 173 (min 3 ✅)
live_return  +83.423%
paper_return  -2.728%
edge         -86.151%   (min_edge_vs_champion_pct 要求 >= 0.0% ❌)
paper_maxdd    8.546%   (上限 10.0% ✅)
```

**三条标准中交易数与回撤都过, 唯独超额收益差 86 个百分点** —— 挑战者参数
(`stop_loss_pct 3.0`) 相对冠军 (`5.0`) 在整个验证期内持续大幅落后, 且趋势稳定
(09-14 edge −85.372% → 09-15 −86.151%), 不是短期噪音。

### 已执行 `learn.py reject`

```
挑战者已否决: {entry.rsi2_max 10.0, exit.rsi2_min 65.0,
              exit.stop_loss_pct 3.0, exit.max_holding_days 10}
```

- `state/learning.json` 的 `challenger` 置 None, `history` 追加 `event=rejected` 留痕
- **冠军留任, 实盘参数零改动** (`stop_loss_pct` 仍为 5.0) —— 否决是"不动实盘"的保守方向
- 否决前已在副本上验证: 无挑战者时 `challenger-config` 打印「无活跃挑战者」rc=0、
  `evaluate` 返回 `no_challenger`, **主跑不会崩**

### 附带证据: 3% 止损在期权口径同样站不住

`weekly_calls.json exit._underlying_stop_note` 早有记载 —— 期权有约 6 倍杠杆,
正股 −5% ≈ 期权 −45%。本次挑战者用更紧的 3% 止损却跑输 86 pp, 与该注记方向一致:
**更紧的止损在本策略上是负贡献, 不是保护**。

### 下一步 (待用户示下, 未自行执行)

`learn.py search` 现已解锁 (原先被活跃挑战者阻塞)。是否要搜下一组挑战者参数、
以及搜哪个方向, 留给用户决定 —— 本会话不自行发起。

## 4. 纸面轨道

- **挑战者**: 净值 $98,125.69。今日出 3 卖 (DIA time_stop · MMM exit_strength ·
  SBUX stop_loss) + 3 买 (LNG / IWM / PFE)。本日之后该轨道无活跃挑战者。
- **动量轮动**: 非调仓日 (昨日刚调仓, 距今 1 天)。
- **周call paper**: 持仓 3 张 (含 09-14 补录的 NVDA260925C00195000)。

## 5. 三处修补的运行状况

- **修法1** (跨日 context 暂存): 连续第 2 日 `anomalies=0`; 实盘期权回写时暂存区
  正常记录 1 条 ✅
- **修法3** (成交日): 本日全部当日成交, `fill_date == today`, 行为与旧版一致 ✅
- **修法2** (排队卖单升市价): 本日 sync 无仍挂卖单, **仍未触发** —— 继续等实战样本
